---
id: installation
title: Installation
---

import { CodeSection } from '../components/CodeSection';
import { Tabs } from '../components/Tabs';

Core package first.

```shell
npm install uniforms
```

Now the schema package.

<Tabs
  group="bridge"
  tabs={[
    { name: 'GraphQL' },
    { name: 'JSON-Schema' },
    { name: 'Simple-Schema' },
    { name: 'Simple-Schema-2' },
    { name: 'Zod' },
  ]}
>
  {({ name: bridgeName }) => (
    <>
      <CodeSection
        language="shell"
        source={`npm install uniforms-bridge-${bridgeName.toLowerCase()}`}
      />
      <p>Finally, pick your theme.</p>
      <Tabs
        group="theme"
        tabs={[
          { name: 'Semantic' },
          { name: 'MUI' },
          { name: 'Bootstrap4' },
          { name: 'Bootstrap5' },
          { name: 'AntD' },
          { name: 'Unstyled' },
        ]}
      >
        {({ name: themeName }) => (
          <>
            <CodeSection
              language="shell"
              source={`npm install uniforms-${themeName.toLowerCase()}`}
            />
            <p>TL;DR; Just let me copy it!</p>
            <CodeSection
              language="shell"
              source={`npm install uniforms uniforms-bridge-${bridgeName.toLowerCase()} uniforms-${themeName.toLowerCase()}`}
            />
          </>
        )}
      </Tabs>
    </>
  )}
</Tabs>

**Note:** If you are going to use a themed package - remember to include correct styles!
