---
id: examples-custom-fields
title: Custom fields
---

import { ExampleCustomizer } from '../website/components/ExampleCustomizer';

### `CompositeField`

This field is a kind of a shortcut for few fields. You can also access all
field props here, like value or onChange for some extra logic.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/CompositeField')}
  example={
    require('../website/pages-parts/CustomFields/CompositeField')
      .CompositeFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/CompositeFieldSchema')}
/>

### `CustomAutoField`

**Note:** Since v3.1, the preferred way is to create an `AutoField` component is to use the `createAutoField` helper. Also, it's often the case that using the [`AutoField.componentDetectorContext`](uth-autofield-algorithm#overriding-autofield) is enough.

These are two _standard_ options to define a custom `AutoField`: either using `connectField` or simply taking the code from the [original one](https://github.com/vazco/uniforms/blob/master/packages/uniforms-unstyled/src/AutoField.tsx#L14-L47) _(theme doesn't matter)_ and simply apply own components and/or rules to render components. Below an example with `connectField`.

```tsx
// Remember to choose a correct theme package
import { AutoField } from 'uniforms-unstyled';

const CustomAuto = props => {
  // This way we don't care about unhandled cases - we use default
  // AutoField as a fallback component.
  const Component = determineComponentFromProps(props) || AutoField;

  return <Component {...props} name="" />;
};

const CustomAutoField = connectField(CustomAuto, {
  initialValue: false
});

// Deprecated: You can also tell your `AutoForm`/`QuickForm`/`ValidatedQuickForm` to use it.
<AutoForm {...props} autoField={CustomAutoField} />;
```

### `CycleField`

This field works as follows: iterate all allowed values and optionally no-value
state if the field is not required. This one uses Semantic-UI.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/CycleField')}
  example={
    require('../website/pages-parts/CustomFields/CycleField').CycleFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/CycleFieldSchema')}
/>

### `DisplayIf`

This simple field component conditionally displays other fields based on input.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/DisplayIf')}
  example={
    require('../website/pages-parts/CustomFields/DisplayIf').DisplayIfForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/DisplayIfSchema')}
/>

### `ImageField`

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/ImageField')}
  example={
    require('../website/pages-parts/CustomFields/ImageField').ImageFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/ImageFieldSchema')}
/>

### `RangeField`

This field works as follows: two datepickers are bound to each other.
Value is a `{start, stop}` object.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/RangeField')}
  example={
    require('../website/pages-parts/CustomFields/RangeField').RangeFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/RangeFieldSchema')}
/>

### `RatingField`

This field works as follows: render stars for each rating and mark them as
filled, if rating (value) is greater.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/RatingField')}
  example={
    require('../website/pages-parts/CustomFields/RatingField').RatingFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/RatingFieldSchema')}
/>

### `SubmitButton`

This field works as follows: render standard submit field and disable it, when
the form is invalid. It's a simplified version of a default SubmitField from
uniforms-unstyled. We use schema from previous examples as a template for validation.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/SubmitField')}
  example={
    require('../website/pages-parts/CustomFields/SubmitField').SubmitFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/SubmitFieldSchema')}
/>

### `SwapField`

This field works as follows: on click of its child (refresh icon) it swaps values of fieldA
and fieldB. It's that simple.

<ExampleCustomizer
  code={require('!!raw-loader!../website/pages-parts/CustomFields/SwapField')}
  example={
    require('../website/pages-parts/CustomFields/SwapField').SwapFieldForm
  }
  schema={require('!!raw-loader!../website/pages-parts/CustomFields/SwapFieldSchema')}
/>
