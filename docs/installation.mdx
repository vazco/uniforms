---
id: installation
title: Installation
---

import {TabsSelect} from './components/Tabs';
import Code from './components/Code';

Core package first.

```shell
npm install uniforms
```

Now the schema package.

<TabsSelect tabs={[{name: 'GraphQL'}, {name: 'JSON-Schema'}, {name: 'Simple-Schema'}, {name: 'Simple-Schema-2'}]}>
  {({name}) => <Code language="shell">{`npm install uniforms-bridge-${name.toLowerCase()}`}</Code>}
</TabsSelect>

Finally, pick your theme.

<TabsSelect
  tabs={[
    {name: 'AntD'},
    {name: 'Bootstrap3'},
    {name: 'Bootstrap4'},
    {name: 'Material'},
    {name: 'Semantic'},
    {name: 'Unstyled'}
  ]}
>
  {({name}) => <Code language="shell">{`npm install uniforms-${name.toLowerCase()}`}</Code>}
</TabsSelect>

**Note:** If you are going to use a themed package - remember to include correct styles!
