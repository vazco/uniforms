---
id: installation
title: Installation
---

import {TabsSelect} from './components/Tabs';
import {Code} from './components/Code';

Core package first.

```shell
npm install --save uniforms
```

Now the schema package.

<TabsSelect
  group="bridge"
  tabs={[{name: 'GraphQL'}, {name: 'JSON-Schema'}, {name: 'Simple-Schema'}, {name: 'Simple-Schema-2'}]}
>
  {({name: bridgeName}) => (
    <>
      <Code language="shell">{`npm install --save uniforms-bridge-${bridgeName.toLowerCase()}`}</Code>
      <br />
      <p>Finally, pick your theme.</p>
      <TabsSelect
        group="theme"
        tabs={[
          {name: 'Semantic'},
          {name: 'Material'},
          {name: 'Bootstrap3'},
          {name: 'Bootstrap4'},
          {name: 'AntD'},
          {name: 'Unstyled'}
        ]}
      >
        {({name: themeName}) => (
          <>
            <Code language="shell">{`npm install --save uniforms-${themeName.toLowerCase()}`}</Code>
            <br />
            <p>TL;DR just let me copy!</p>
            <Code language="shell">
              {`npm install --save uniforms uniforms-bridge-${bridgeName.toLowerCase()} uniforms-${themeName.toLowerCase()}`}
            </Code>
          </>
        )}
      </TabsSelect>
    </>
  )}
</TabsSelect>

**Note:** If you are going to use a themed package - remember to include correct styles!
