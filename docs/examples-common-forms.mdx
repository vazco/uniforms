---
id: examples-common-forms
title: Common forms
---

import ExampleCustomizer from '../website/scripts/components/Examples/ExampleCustomizer';
import TogglerTabs from '../website/scripts/components/TogglerTabs';
import {code} from '../website/scripts/components/Code';

import SignUpForm from '../website/scripts/components/Examples/CommonForms/SignUp';
import SignUpSchema from './example_schemas/SignUpSchema.md';

### `Sign up`

<ExampleCustomizer
  code={theme =>
    code(
      'js',
      `import React from 'react';\n
import schema from './SignUpSchema';
import { AutoForm } from 'uniforms-${theme}';\n
export default function SignUpForm() {
  return (
    <AutoForm
      schema={schema}
      onSubmit={model => alert(JSON.stringify(model, null, 2))}
    />
  );
}`
    )
  }
  example={<SignUpForm />}
  schema={<SignUpSchema />}
/>
