import {TabsSelect} from './components/Tabs';
import tabs from './components/tabsData';

# This is Tabs!

<TabsSelect tabs={tabs}>
  {({name, content}) => (
    <>
      {content}
      <h3>{name}</h3>
    </>
  )}
</TabsSelect>
