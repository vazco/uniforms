---
id: tabs
title: Test Tabs
---

import {TabsSelect} from './components/Tabs';
import JSONMdx from './tutorials-basic-uniforms-usage.mdx';
import {Code} from './components/Code';
import {Toggler} from './components/Toggler';
import {Code as IconCode, Box} from 'react-feather';
import {TogglerTabs} from './components/TogglerTabs';

# This is MDX file with dynamic tabs!

<TabsSelect
  tabs={[
    {name: 'SimpleSchema', content: <h1>SimpleSchema</h1>},
    {name: 'JSON', content: <JSONMdx />},
    {name: 'GraphQL', content: <h1>GraphQL</h1>}
  ]}
>
  {({name, content}) => content}
</TabsSelect>

<TabsSelect
  tabs={[
    {name: 'SimpleSchema', content: <h1>SimpleSchema</h1>},
    {name: 'JSON', content: <JSONMdx />},
    {name: 'GraphQL', content: <h1>GraphQL</h1>}
  ]}
>
  {({name, content}) => content}
</TabsSelect>

<TogglerTabs
  tabsItems={[{name: 'SimpleSchema'}, {name: 'JSON'}, {name: 'GraphQL'}]}
  togglerItems={[
    {
      name: 'code',
      tooltipText: 'Toggle source code',
      icon: <IconCode />,
      component: <Code language="html">{`<h1>Example output html</h1>`}</Code>
    },
    {
      name: 'example',
      tooltipText: 'Toggle example',
      icon: <Box />,
      component: <h1>Example output html</h1>
    }
  ]}
>
  {({tab, toggle}) => (
    <section>
      {tab && tab.name}
      {toggle && toggle.name}
    </section>
  )}
</TogglerTabs>

<Toggler
  items={[
    {
      name: 'code',
      tooltipText: 'Toggle source code',
      icon: <IconCode />,
      component: <Code language="html">{`<h1>Example output html</h1>`}</Code>
    },
    {
      name: 'example',
      tooltipText: 'Toggle example',
      icon: <Box />,
      component: <h1>Example output html</h1>,
      active: true
    }
  ]}
/>
